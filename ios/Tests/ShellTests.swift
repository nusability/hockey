import Testing

/// Placeholder so the test target exists from the first commit; the first real suite is the
/// golden-vector replay (shared/vectors).
@Test func shellBuilds() {
    #expect(Bool(true))
}
